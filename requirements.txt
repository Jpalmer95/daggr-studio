# Runtime dependencies for the Daggr Studio Space (docker SDK).
#
# Pinned deliberately: this app generates daggr code for other people, so the versions it
# runs on must be the versions it was tested with (daggr 0.8.0 + gradio 6.27.0 locally).
#
# Do NOT pin gradio_client: gradio 6.27 pins gradio-client==2.7.0 itself, and pinning 2.5.0
# makes the whole image unresolvable (see MASTER_PLAN §4 "build failures hit and fixed").
daggr==0.8.0
gradio==6.27.0

fastapi>=0.115.0
uvicorn[standard]>=0.34.0
huggingface_hub>=1.17.0
pillow>=10.0.0
networkx>=3.0

# Tests ship with the image so a deployed Space can self-check (`python -m pytest -q`) -
# far cheaper than guessing from build logs.
pytest>=8.0.0
pytest-asyncio>=1.0.0
httpx>=0.27.0
